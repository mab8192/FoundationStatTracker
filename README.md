# Foundation Stat Tracker

This is a mod for the game "Foundation" that adds stat trackers for resource production and consumption, averaged on a per-day, per-week, and per-month basis.

## Installation
Place the `mod.lua` and `mod.json` files in a new folder within the `mods` folder of you Foundation install.

